<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:theme="@style/PopupMenu">

    <LinearLayout
        android:id="@+id/rect_nfc_read"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:layout_alignParentTop="true"
        android:background="@drawable/bg_rect_nfc"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="24dp">

        <TextView
            android:id="@+id/ready_to_sc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/marginkelas"
            android:text="@string/ready_to_scan"
            android:textAppearance="@style/ready_to_scan"
            android:gravity="center_vertical"
            />

        <ImageView
            android:id="@+id/nfc_phone"
            android:layout_width="125dp"
            android:layout_height="150dp"
            android:layout_margin="@dimen/marginkelas"
            android:background="@drawable/ic_nfc_phone"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/dekatkan_pe"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/marginkelas"
            android:text="@string/dekatkan_kartu_tag"
            android:textAppearance="@style/dekatkan_kartu_tag"
            android:gravity="center_vertical"
            />

        <Button
            android:id="@+id/bt_scan_nfc_cancel"
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:background="@drawable/button_blue_wide"
            android:text="@string/label_cancel"
            android:textAppearance="@style/label_button"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/iv_nfc" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>